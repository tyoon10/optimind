__pycache__/
*.pyc
.env
.venv
venv/
*.json
!user_memory.json
# Schemas and config are contracts, never personal data — keep them tracked
# despite the blanket *.json ignore above.
!schemas/*.json
!.mcp.json
!**/package.json
!**/tsconfig.json
!routines/*.json
logs/
bin/cloudflared.exe
*.log
!.claude/settings.json

# Personal data — never commit
data/
Gemini conversation history/

# Build artifacts
*.egg-info/
