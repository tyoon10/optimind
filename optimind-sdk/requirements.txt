# OptiMind SDK — local/self-hosted deployment path (Slack + FastAPI server removed in 4.0.0)
# In-process Agent SDK runtime (src/agent.py) + reference tool/hook implementations.
claude-agent-sdk>=0.1.48

# Standalone stdio MCP server (bin/optimind_mcp_server.py), registered in the
# repo-root ../.mcp.json (one level up from this directory, not co-located here).
mcp>=1.0.0

# Preserved from v1
python-dotenv>=1.0.0
pytz>=2024.1
gitpython>=3.1.40
