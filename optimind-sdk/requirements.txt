# OptiMind SDK — local-CLI / reference layer (Slack + FastAPI server removed)
claude-agent-sdk>=0.1.48

# Standalone stdio MCP server (bin/optimind_mcp_server.py) exposed via .mcp.json
mcp>=1.0.0

# Preserved from v1
python-dotenv>=1.0.0
pytz>=2024.1
gitpython>=3.1.40
