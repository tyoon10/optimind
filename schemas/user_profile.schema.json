{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/tyoon10/optimind/schemas/user_profile.schema.json",
  "title": "OptiMind User Profile",
  "description": "Canonical schema for user_profile.json. The runtime MUST validate the loaded profile against this schema and refuse to start on incompatibility. PENDING semantics: any rule with confidence < 0.5 is treated as machine-readable PENDING (not just human convention).",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "user_id", "name", "rules"],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "1.0",
      "description": "Schema version. Bump via a migrations/user_profile_<from>to<to>.py script."
    },
    "user_id": {
      "type": "string",
      "minLength": 1
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "rules": {
      "type": "array",
      "items": { "$ref": "#/$defs/PreferenceRule" }
    }
  },
  "$defs": {
    "PreferenceRule": {
      "type": "object",
      "additionalProperties": false,
      "required": ["topic", "rule", "confidence"],
      "properties": {
        "topic": {
          "type": "string",
          "enum": ["nutrition", "scheduling", "profile", "environment"],
          "description": "Enforced enum so a typo (e.g. 'nuturition') fails validation rather than silently creating an orphan rule the Analyst agent will never grep."
        },
        "rule": {
          "type": "string",
          "minLength": 1
        },
        "source": {
          "type": "string",
          "enum": ["user_interaction", "agent_learned", "manual"],
          "default": "user_interaction"
        },
        "created_at": {
          "type": "string",
          "format": "date-time",
          "description": "ISO-8601 datetime with explicit offset. Bare 'Z' (UTC) suffix is banned — every timestamp must carry the user's offset (US/Eastern by default) per the timezone binding in CLAUDE.md."
        },
        "updated_at": {
          "type": "string",
          "format": "date-time",
          "description": "Same offset rule as created_at."
        },
        "confidence": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Below 0.5 is the PENDING threshold: the rule is machine-readable but not yet authoritative. Agents should surface PENDING rules for confirmation rather than apply them silently."
        }
      }
    }
  }
}
